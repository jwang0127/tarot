# Tarot Web｜响应式塔罗抽牌网站 PRD 与 Codex 开发说明

> 目标仓库：<https://github.com/jwang0127/tarot>  
> 参考能力：<https://github.com/shan7777/tarot.skill>  
> 文档版本：V1.0  
> 产品形态：手机端优先、兼容电脑端的响应式 Web App  
> AI 服务：DeepSeek API  
> 目标用途：可直接交给 Codex 按阶段开发  
> 当前仓库状态：空仓库，需要从零初始化

---

# 1. 项目定位

## 1.1 一句话介绍

一个拥有沉浸式洗牌、选牌、翻牌、音效和 AI 解读体验的现代塔罗网站。

## 1.2 产品目标

1. 用户无需注册，也能在 45 秒内完成第一次抽牌。
2. 手机端单手即可完成“提问—洗牌—选牌—翻牌—查看解读”。
3. 电脑端拥有更完整的牌桌空间、扇形牌阵和轻量视差效果。
4. DeepSeek 只负责解释已经抽出的牌，不参与随机抽牌。
5. 塔罗结果用于娱乐、自我探索和反思，不输出确定性预言。
6. 页面兼顾神秘感、现代审美、流畅交互和可读性。
7. 首个版本可以直接部署到 Vercel 或兼容 Node.js 的平台。

## 1.3 产品关键词

```text
神秘
高级
安静
克制
沉浸
私密
有仪式感
可回看
不迷信
```

## 1.4 核心原则

> 随机模块负责抽牌，AI 负责解读，用户负责决定。

---

# 2. 参考 tarot.skill 的能力映射

参考 Skill 中已有以下能力：

- 韦特塔罗。
- 托特塔罗。
- 单张指引。
- 过去 / 现在 / 未来三牌阵。
- 情况 / 障碍 / 建议三牌阵。
- 五芒星牌阵。
- 凯尔特十字牌阵。
- 随机抽牌。
- 用户控制洗牌次数。
- 用户自己填写已抽到的牌。
- 正位与逆位。
- 神秘诗意型解读。
- 理性分析型解读。
- 温暖陪伴型解读。
- 混合型解读。
- 自定义象征牌。
- 单牌含义、牌组互动、综合解读和行动建议。

网站中对应实现：

| Skill 能力 | 网页实现 |
|---|---|
| 韦特 / 托特 | 高级设置中的牌系切换 |
| 单张牌 | 每日一牌、即时指引 |
| 三牌阵 | 快速占卜默认模式 |
| 五芒星 | 多维度分析 |
| 凯尔特十字 | 深度解读 |
| AI 随机抽牌 | 改为浏览器或服务端安全随机数 |
| 指定洗牌次数 | 作为仪式感交互，不声称提升准确率 |
| 用户自报牌 | “我已经线下抽好牌”模式 |
| 正逆位 | 每张牌独立随机，默认逆位概率 30% |
| 四种风格 | 作为 DeepSeek Prompt 参数 |
| 自定义象征牌 | P1 版本实现 |
| 行动建议 | 结果页固定模块 |

## 2.1 不直接照搬的部分

原 Skill 中引用本地 Windows 路径或其他人物 Persona Skill 的能力，不在网页 MVP 中使用。

MVP 不支持：

- 读取本地 `D:\Claude-work\...`。
- 模仿前任、老板、明星或其他真实人物。
- 读取外部私人 Persona 文件。
- 让 AI 为了“更符合问题”而改牌或重抽。

后续若需要人格化表达，应使用平台内置虚构语气包：

- 月夜诗人。
- 理性咨询师。
- 温暖陪伴者。
- 极简直觉派。

---

# 3. 推荐技术方案

## 3.1 技术栈

由于需要安全调用 DeepSeek API，不能把 API Key 放在前端。

推荐：

```text
框架：Next.js，使用 App Router
语言：TypeScript
样式：Tailwind CSS
动画：Framer Motion
音效：Howler.js 或原生 Web Audio API
状态管理：Zustand
本地记录：IndexedDB，建议使用 Dexie
数据校验：Zod
图标：Lucide
部署：Vercel
```

## 3.2 为什么不建议纯静态 HTML

纯静态网页无法安全隐藏 DeepSeek API Key。任何写在浏览器端的 Key 都可能被用户查看和盗用。

正确架构：

```text
浏览器
  ↓
Next.js Server Route
  ↓
DeepSeek API
```

浏览器永远不直接拿到 DeepSeek API Key。

## 3.3 环境变量

创建：

```bash
.env.local
```

内容：

```env
DEEPSEEK_API_KEY=你的_API_Key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=填写实际使用的模型名称
```

要求：

- `.env.local` 必须加入 `.gitignore`。
- 禁止把 Key 写入前端代码。
- 禁止把 Key 提交到 GitHub。
- 服务端读取 `process.env.DEEPSEEK_API_KEY`。
- 部署时在 Vercel 项目环境变量中填写。

由于模型名称、API 格式和价格可能更新，实现前应以 DeepSeek 当前官方文档为准，不在代码中依赖未经确认的旧版本字段。

---

# 4. 用户画像

## 4.1 快速体验用户

特点：

- 第一次接触塔罗。
- 从社交平台或朋友分享进入。
- 只想快速问感情、工作或今日状态。
- 不想学习复杂牌义。

需要：

- 不注册也能用。
- 操作直观。
- 页面好看。
- 结果有共鸣。
- 可以保存和分享。

## 4.2 情绪陪伴用户

特点：

- 正在经历关系、职业或生活困惑。
- 希望得到梳理，但不想被吓到。

需要：

- 语气温和。
- 不评判。
- 不宿命。
- 有现实行动建议。
- 问题默认私密。

## 4.3 塔罗爱好者

特点：

- 理解牌阵、正逆位和不同牌系。
- 对牌义和组合关系要求较高。

需要：

- 自定义牌阵。
- 自定义正逆位。
- 查看传统牌义。
- 保存历史记录。
- 支持线下抽牌后录入。

---

# 5. MVP 功能范围

## 5.1 P0 必须完成

1. 响应式首页。
2. 无登录快速体验。
3. 每日一牌。
4. 自定义问题。
5. 主题选择。
6. 五种牌阵。
7. 韦特塔罗。
8. 托特塔罗。
9. 四种解读风格。
10. 正逆位。
11. 洗牌动画。
12. 选牌交互。
13. 翻牌动画。
14. 完整音效系统。
15. DeepSeek 解读接口。
16. 结构化解读结果。
17. 本地历史记录。
18. 结果分享图。
19. 手机端适配。
20. 电脑端适配。
21. API 限流。
22. 安全过滤。
23. 错误降级。
24. 无障碍与减少动态效果。

## 5.2 P1

- 账号登录。
- 云端同步。
- 收藏牌局。
- 自定义象征牌。
- 牌义百科。
- 每日一牌连续记录。
- 针对结果继续追问。
- PWA 安装。
- 多语言。
- 多套牌背。
- 多套环境音。
- 虚构语气包。

## 5.3 P2

- 高级牌阵编辑器。
- 年度回顾。
- 真人塔罗师服务。
- 社区。
- 付费订阅。
- 用户私人牌库。

---

# 6. 信息架构

```text
/
├── 首页
├── /reading/new
│   ├── 选择主题
│   ├── 输入问题
│   ├── 选择牌阵
│   ├── 高级设置
│   ├── 洗牌
│   ├── 选牌
│   └── 翻牌
├── /reading/[id]
│   ├── 牌阵总览
│   ├── 一句话主题
│   ├── 单牌解读
│   ├── 牌组互动
│   ├── 综合解读
│   ├── 行动建议
│   └── 分享
├── /daily
│   └── 每日一牌
├── /history
│   └── 本地历史
├── /cards
│   └── 牌义百科
├── /settings
│   ├── 声音
│   ├── 动效
│   ├── 默认牌系
│   └── 隐私
└── /about
    ├── 塔罗如何工作
    ├── 使用边界
    ├── 隐私说明
    └── 免责声明
```

---

# 7. 核心流程

## 7.1 快速占卜

```text
首页
→ 点击“开始抽牌”
→ 选择主题
→ 输入问题
→ 默认三牌阵
→ 洗牌
→ 选择 3 张牌
→ 逐张翻牌
→ DeepSeek 生成解读
→ 保存 / 分享 / 再抽一次
```

## 7.2 每日一牌

```text
首页
→ 点击“今日指引”
→ 简短洗牌
→ 选择 1 张
→ 翻牌
→ 显示今日关键词、提醒、行动建议
→ 保存到本地历史
```

## 7.3 自定义模式

```text
开始抽牌
→ 输入问题
→ 展开高级设置
→ 选择牌系
→ 选择牌阵
→ 选择解读风格
→ 开启或关闭逆位
→ 选择抽牌方式
→ 开始洗牌
```

## 7.4 用户已在线下抽牌

```text
开始抽牌
→ 选择“我已经抽好牌”
→ 搜索并选择牌名
→ 设置正位或逆位
→ 分配牌阵位置
→ DeepSeek 生成解读
```

---

# 8. 首页设计

## 8.1 主文案

标题：

> 把问题交给牌，把答案留给自己

副标题：

> 一次安静、私密、可回看的塔罗自我探索

主按钮：

> 开始抽牌

次按钮：

> 抽取今日一牌

信任说明：

> 无需注册 · 默认不公开问题 · 仅用于娱乐与自我探索

## 8.2 手机端

- 单栏布局。
- 顶部为 Logo 和菜单按钮。
- 中间展示三张错位牌背。
- 主按钮占满内容宽度。
- 次按钮放在主按钮下方。
- 页面首屏不强制写死为 `100vh`，避免手机浏览器地址栏造成跳动。
- 底部展示隐私和免责声明。

## 8.3 电脑端

- 左侧为文案和按钮。
- 右侧为三张具有轻微 3D 视差的牌背。
- 最大内容宽度 1280px。
- 鼠标移动时牌面旋转不超过 4 度。
- 背景使用极低频率的星尘和柔和光晕。
- 不允许满屏高频粒子。

---

# 9. 问题输入页

标题：

> 这次，你想看清什么？

## 9.1 快捷主题

- 感情关系。
- 事业选择。
- 财务状态。
- 人际关系。
- 自我成长。
- 开放式指引。

## 9.2 输入规则

- 最少 2 个字符。
- 建议不超过 200 个中文字符。
- 支持跳过问题，选择开放式指引。
- 不要求真实姓名、生日、电话、身份证。
- 实时显示字符数。
- 只有用户点击生成解读后，问题才发送给服务端。
- 不把完整问题放到 URL。

## 9.3 推荐问题

- 我在这段关系中最需要看清什么？
- 当前工作困境真正的阻碍是什么？
- 接下来一个月，我应该把精力放在哪里？
- 面对这次选择，我忽略了什么？
- 我可以如何改善目前的状态？

## 9.4 高风险问题改写

对于医疗、法律、投资、死亡或读取第三方隐私的问题，提示：

> 这个问题涉及无法由塔罗可靠判断的事实。你可以改问：“面对这件事，我可以关注哪些现实信号和行动？”

不直接输出确定结论。

---

# 10. 牌阵选择

| 牌阵 | 张数 | 用途 |
|---|---:|---|
| 单张指引 | 1 | 今日提醒、即时灵感 |
| 过去 / 现在 / 未来 | 3 | 事件发展 |
| 情况 / 障碍 / 建议 | 3 | 困境与决策 |
| 五芒星 | 5 | 多维度综合分析 |
| 凯尔特十字 | 10 | 深度复杂问题 |

## 10.1 交互要求

- 点击卡片选中。
- 选中态包含金色边框、抬升和勾选图标。
- 卡片展示迷你牌阵预览。
- 手机端纵向排列。
- 电脑端两至三列。
- 凯尔特十字标注“深度解读，约 3–5 分钟”。

---

# 11. 高级设置

默认收起。

## 11.1 牌系

- 韦特塔罗。
- 托特塔罗。

## 11.2 解读风格

- 神秘诗意。
- 理性分析。
- 温暖陪伴。
- 混合型。

## 11.3 逆位

- 开启，默认概率 30%。
- 关闭。

## 11.4 抽牌方式

- 用户自由选牌。
- 系统代抽。
- 用户已经线下抽牌。

## 11.5 洗牌体验

- 简短。
- 标准。
- 仪式感。

说明：

> 洗牌次数只增强参与感，不声称次数越多越准确。

---

# 12. 洗牌页

## 12.1 状态机

```text
idle
→ preparing
→ shuffling
→ ready
→ selecting
```

## 12.2 手机端

- 中央为牌堆。
- 用户可左右滑动牌堆触发洗牌。
- 也可点击按钮自动洗牌。
- 洗牌完成后展开为横向可拖动卡带。
- 支持轻微震动反馈。
- 用户开启减少动态效果时，自动禁用复杂动画和震动。

## 12.3 电脑端

- 牌堆位于中央牌桌。
- 洗牌分为交错、收拢、展开三个阶段。
- 用户可点击或拖动触发。
- 完成后展开为扇形牌列。

## 12.4 动效要求

- 标准洗牌总时长 1.6–2.2 秒。
- 使用 `transform` 和 `opacity`。
- 禁止使用高频改变 `top/left` 的动画。
- 目标 60fps。
- 提供“跳过动画”。
- 遵守 `prefers-reduced-motion`。

---

# 13. 选牌页

## 13.1 核心原则

用户必须明确感知：

> 这是我选择的牌。

## 13.2 手机端

- 牌背横向滚动。
- 中央选择区域带柔和光圈。
- 点击后牌面上移，进入已选槽位。
- 顶部显示 `已选择 2 / 3`。
- 支持撤销最近一次选择。
- 已选牌不立即翻面。

## 13.3 电脑端

- 牌背形成扇形。
- hover 时轻微抬升。
- 点击后移动至牌阵占位。
- hover 不得改变卡牌顺序。
- 触屏电脑也必须能直接点击。

## 13.4 随机规则

- 同一牌局不能重复抽到同一张牌。
- 牌序和正逆位在牌堆生成时已经确定。
- 用户选择的是牌堆中的位置。
- DeepSeek 不参与随机过程。
- 每次牌局保存：
  - `deckSeed`。
  - 洗牌算法版本。
  - 选中索引。
  - 正逆位。
  - 牌阵位置。

推荐使用：

```ts
crypto.getRandomValues()
```

禁止使用仅基于：

```ts
Math.random()
```

作为唯一随机来源。

---

# 14. 翻牌页

## 14.1 流程

1. 已选牌移动至牌阵位置。
2. 每个位置显示含义。
3. 用户逐张点击翻牌。
4. 卡片执行 3D 翻转。
5. 显示牌名、正逆位和位置。
6. 用户可点击“全部揭晓”。
7. 全部翻开后出现“开始解读”。

## 14.2 动画参数

- 翻牌时长 600–800ms。
- 使用 `rotateY(180deg)`。
- 使用 `backface-visibility: hidden`。
- 牌名在翻牌完成约 120ms 后淡入。
- 逆位牌图真实旋转 180 度。
- 文字标签始终保持正常方向。
- 最后一张翻开后延迟 300–500ms 出现解读按钮。

---

# 15. 音效系统

音效是本项目的重要组成部分，但必须克制、可关闭，并符合浏览器自动播放规则。

## 15.1 音效类型

| 场景 | 音效 | 体验要求 |
|---|---|---|
| 页面进入 | 极轻环境氛围 | 默认不自动播放 |
| 按钮点击 | 柔和木质轻点 | 音量很低 |
| 洗牌 | 纸牌摩擦和交错 | 与动画同步 |
| 牌堆收拢 | 轻微纸张合拢 | 时长短 |
| 选牌 | 柔和抽卡声 | 不使用游戏中奖音 |
| 放入牌阵 | 轻微落桌声 | 有空间感 |
| 翻牌 | 短促纸牌翻转声 | 与翻转中点同步 |
| 正位揭晓 | 柔和金属泛音 | 非必须 |
| 逆位揭晓 | 稍低的木质泛音 | 不制造恐怖感 |
| 全部揭晓 | 一次短钟声或水晶音 | 时长不超过 1.5 秒 |
| AI 解读完成 | 极轻提示音 | 不打扰用户 |
| 错误 | 低沉轻提示 | 不尖锐 |
| 分享成功 | 轻快确认音 | 时长短 |

## 15.2 环境音

可选环境音：

- 月夜风声。
- 极轻壁炉声。
- 远处雨声。
- 安静天文台环境。

要求：

- 默认关闭，或首次通过明确按钮开启。
- 禁止进入页面后自动播放。
- 循环点必须平滑。
- 不包含人声。
- 不使用可能引发惊吓的低频。
- 环境音和操作音分开控制。

## 15.3 声音设置

设置页提供：

- 总声音开关。
- 环境音开关。
- 操作音开关。
- 总音量。
- 环境音音量。
- 操作音音量。
- 恢复默认。

建议默认值：

```text
总声音：关闭，等待用户主动开启
环境音：关闭
操作音：开启，但需在用户第一次交互后解锁
环境音量：15%
操作音量：35%
```

也可以在首页提供：

> 开启沉浸音效

用户点击后才初始化音频上下文。

## 15.4 浏览器规则

必须遵守：

- 未经用户手势，不自动播放带声音媒体。
- 第一次点击“开启声音”后才调用音频解锁。
- 页面切换时不要重复创建 AudioContext。
- 标签页进入后台后暂停环境音。
- 用户返回前台后，不自动恢复大音量。
- iOS Safari 必须经过用户手势后播放。
- 音频加载失败不能阻断抽牌流程。

## 15.5 无障碍

- 声音不能作为唯一反馈。
- 每个声音事件都必须有视觉反馈。
- 用户关闭声音后，流程完整可用。
- 系统开启减少动态效果时，可同时建议关闭震动。
- 不使用突然放大的音效。
- 不使用惊吓音。

## 15.6 音频资源规范

推荐目录：

```text
/public/audio/
├── ambience/
│   ├── night-wind.mp3
│   ├── quiet-rain.mp3
│   └── observatory.mp3
├── cards/
│   ├── shuffle-01.mp3
│   ├── shuffle-02.mp3
│   ├── draw.mp3
│   ├── place.mp3
│   └── flip.mp3
├── ui/
│   ├── tap.mp3
│   ├── success.mp3
│   ├── reveal.mp3
│   └── error.mp3
└── README.md
```

要求：

- 优先使用拥有明确商用许可的音频。
- 保存来源、作者和许可证。
- 不使用来源不明的网上音效。
- 尽量压缩体积。
- 操作音建议单个低于 100KB。
- 环境音建议延迟加载。
- 首屏不预加载全部环境音。

## 15.7 Sound Manager

建议封装：

```ts
type SoundName =
  | "tap"
  | "shuffle"
  | "draw"
  | "place"
  | "flip"
  | "reveal"
  | "success"
  | "error";

type SoundSettings = {
  enabled: boolean;
  ambienceEnabled: boolean;
  effectsEnabled: boolean;
  masterVolume: number;
  ambienceVolume: number;
  effectsVolume: number;
};

interface SoundManager {
  unlock(): Promise<void>;
  play(name: SoundName): void;
  startAmbience(name: string): void;
  stopAmbience(): void;
  setSettings(settings: SoundSettings): void;
  preloadCore(): Promise<void>;
}
```

音效调用必须与动画状态绑定，而不是随意使用 `setTimeout`。

例如：

```text
洗牌动画开始 → 播放 shuffle
卡牌离开牌堆 → 播放 draw
卡牌落到牌阵 → 播放 place
翻牌旋转到 90 度 → 播放 flip
全部翻牌结束 → 播放 reveal
```

---

# 16. AI 加载体验

禁止一直显示机械旋转图标。

使用分阶段文案：

1. 正在理解你的问题。
2. 正在连接牌面之间的线索。
3. 正在整理可以行动的建议。

要求：

- 文案只是表示处理阶段，不宣称正在接收宇宙能量。
- 加载期间仍显示用户抽到的牌。
- API 超时时显示重试按钮。
- 不允许超时后重新抽牌。
- 重试时沿用完全相同的牌和正逆位。

---

# 17. 结果页

## 17.1 页面结构

1. 用户问题。
2. 牌阵总览。
3. 一句话主题。
4. 单牌解读。
5. 牌组互动。
6. 综合叙事。
7. 行动建议。
8. 反思问题。
9. 免责声明。
10. 保存、分享、重新抽牌。

## 17.2 桌面端

- 左侧固定牌阵。
- 右侧显示摘要和解读。
- 向下滚动时牌阵可轻微吸顶。
- 凯尔特十字必须完整显示，不能横向溢出。

## 17.3 手机端

顺序：

```text
牌阵
→ 一句话主题
→ 综合摘要
→ 单牌卡片
→ 牌组互动
→ 行动建议
```

不要把所有文字一次塞满首屏。

## 17.4 单牌卡片

包括：

- 牌图。
- 中文牌名。
- 英文牌名。
- 正位或逆位。
- 牌阵位置。
- 2–3 个关键词。
- 结合问题的解释。
- 传统牌义折叠内容。

## 17.5 牌组互动

必须单独解释：

- 牌之间的呼应。
- 牌之间的冲突。
- 叙事变化。
- 大阿尔卡那占比。
- 重复元素。
- 主要主题。

禁止只逐张解释后简单拼接“综合来看”。

## 17.6 行动建议

生成 1–3 条。

每条必须：

- 用户自己可以执行。
- 不依赖他人一定配合。
- 不包含高风险操作。
- 不使用“马上辞职”“立即分手”“立刻买入”等决定性命令。
- 尽量有现实时间范围。

## 17.7 固定免责声明

> 塔罗呈现的是当下视角与可能性，不是固定命运。重要决定请结合现实信息和专业意见。

---

# 18. DeepSeek 接入架构

## 18.1 服务端接口

建议：

```text
POST /api/readings/interpret
```

请求流程：

```text
前端提交问题、牌阵和已抽出的牌
→ 服务端校验
→ 安全分类
→ 组合 Prompt
→ 调用 DeepSeek
→ 校验 JSON
→ 返回前端
```

## 18.2 API Key 安全

必须：

- 仅在服务端调用 DeepSeek。
- 不在浏览器网络请求中出现 API Key。
- 不在日志中打印 API Key。
- 不在错误信息中返回完整请求头。
- 不提交 `.env.local`。
- 部署平台使用加密环境变量。

## 18.3 请求输入

```ts
type ReadingRequest = {
  readingId: string;
  locale: "zh-CN";

  question: string;

  topic:
    | "love"
    | "career"
    | "finance"
    | "relationship"
    | "growth"
    | "open";

  deckSystem:
    | "rider-waite"
    | "thoth";

  spread: {
    id:
      | "single"
      | "past-present-future"
      | "situation-obstacle-advice"
      | "pentagram"
      | "celtic-cross";

    positions: Array<{
      key: string;
      label: string;
      meaning: string;
    }>;
  };

  cards: Array<{
    id: string;
    nameZh: string;
    nameEn: string;
    orientation: "upright" | "reversed";
    positionKey: string;
    canonicalMeaningUpright: string[];
    canonicalMeaningReversed: string[];
  }>;

  style:
    | "mystic"
    | "rational"
    | "warm"
    | "balanced";
};
```

## 18.4 输出结构

```ts
type ReadingResponse = {
  status:
    | "ok"
    | "needs_reframe"
    | "blocked";

  safetyMessage?: string;

  theme: {
    title: string;
    summary: string;
    keywords: string[];
  };

  cardReadings: Array<{
    cardId: string;
    positionKey: string;
    visualSymbols: string[];
    coreMeaning: string;
    contextualMeaning: string;
    shadowOrBlock?: string;
    practicalPrompt: string;
  }>;

  interactions: {
    narrativeArc: string;
    harmonies: string[];
    tensions: string[];
    dominantPatterns: string[];
  };

  synthesis: {
    short: string;
    full: string;
  };

  actions: Array<{
    title: string;
    detail: string;
    timeframe?: string;
  }>;

  reflectionQuestions: string[];

  closing: string;

  disclaimer: string;
};
```

## 18.5 Zod 校验

服务端必须：

- 使用 Zod 校验请求。
- 使用 Zod 校验模型输出。
- 缺字段时自动重试一次。
- 第二次仍失败时使用本地降级模板。
- 限制每个文本字段长度。
- 转义输出内容。
- 不渲染 AI 返回的任意 HTML。
- 不执行 AI 返回的代码。

## 18.6 系统提示词

```text
你是一位克制、专业、以自我探索为导向的塔罗解读者。

你将收到：
1. 用户的问题；
2. 已由独立随机模块抽出的牌；
3. 每张牌的正位或逆位；
4. 每张牌在牌阵中的位置；
5. 用户选择的解读风格；
6. 每张牌的基础牌义资料。

必须遵守：

- 不改变、不补充、不重新抽取任何牌。
- 不声称牌是由你抽出的。
- 结合问题、牌义和牌阵位置进行解释。
- 多张牌必须分析互动和叙事。
- 逆位不是简单负面，要解释为阻碍、内化、延迟或转化。
- 使用引导性语言，不使用确定性预言。
- 不宣称读取第三方真实内心或隐私。
- 不给出医疗诊断、法律结论、投资操作或死亡预测。
- 对高风险问题，引导用户改写成“我可以如何面对或行动”。
- 建议必须现实、可执行，并由用户自己完成。
- 输出必须严格符合指定 JSON Schema。
```

## 18.7 解读风格

### 神秘诗意

- 使用意象和象征。
- 允许轻量隐喻。
- 不故弄玄虚。
- 建议部分必须回到现实。

### 理性分析

- 使用模式、资源、边界、风险、选择等语言。
- 强调可验证信息。
- 适合职业和决策问题。

### 温暖陪伴

- 先共情。
- 避免批判。
- 不夸大危险。
- 给用户安全感。

### 混合型

- 摘要和意象部分偏诗意。
- 分析部分偏理性。
- 建议部分务实。

---

# 19. 高风险内容处理

## 19.1 医疗

不能回答：

- 我是不是得了某种疾病？
- 治疗能不能成功？
- 我应该停药吗？

改写：

> 面对健康焦虑，我可以怎样整理信息并寻求合适帮助？

## 19.2 法律

不能回答：

- 官司一定能赢吗？
- 对方会不会坐牢？

改写：

> 面对当前法律争议，我需要关注哪些现实信息和准备？

## 19.3 投资与博彩

不能回答：

- 哪只股票会涨？
- 彩票号码是什么？
- 我应该买多少？

改写：

> 面对这项财务决定，我可能忽略了哪些风险和情绪因素？

## 19.4 死亡与自伤

不输出死亡时间、方式或宿命结论。

若用户表达现实中的自伤或他伤风险，不继续塔罗流程，改为安全支持和寻求现实帮助。

## 19.5 第三方隐私

不能声称知道：

- 对方现在在想什么。
- 对方是否正在出轨。
- 对方具体在哪里或在做什么。

可以改为：

> 我可以如何观察这段关系中的现实信号，并保护自己的边界？

---

# 20. 牌库数据结构

建议：

```ts
type TarotCard = {
  id: string;
  system: "rider-waite" | "thoth";
  arcana: "major" | "minor";
  suit?: "wands" | "cups" | "swords" | "pentacles";
  rank?: string;
  number?: number;
  nameZh: string;
  nameEn: string;
  image: string;
  keywordsUpright: string[];
  keywordsReversed: string[];
  meaningUpright: string;
  meaningReversed: string;
  symbols: string[];
  element?: string;
  astrology?: string;
};
```

数据文件：

```text
/src/data/cards/
├── rider-waite.json
├── thoth.json
└── schema.ts
```

要求：

- 每套牌严格 78 张。
- 牌 ID 稳定。
- 图片路径和牌义分离。
- 前端不能依赖中文牌名作为唯一 ID。
- 编写自动测试验证：
  - 总数 78。
  - ID 不重复。
  - 图片路径存在。
  - 正逆位牌义不为空。

卡牌图片必须使用拥有明确许可的素材。生产上线前保存素材来源和许可证说明。

---

# 21. 历史记录

## 21.1 MVP

使用 IndexedDB。

保存：

- 时间。
- 问题摘要。
- 主题。
- 牌阵。
- 抽到的牌。
- 正逆位。
- 解读风格。
- AI 结果。
- 是否收藏。
- Seed 和算法版本。

## 21.2 隐私

- 默认仅保存在当前设备。
- 提供“清空全部历史”。
- 不把完整问题放进 URL。
- 不默认上传历史到服务器。
- 不使用用户问题训练任何自有模型。
- 若服务端保留日志，必须去除问题原文或做最小化处理。

## 21.3 数量

- 默认最多保存 100 条。
- 超过后优先删除未收藏的最早记录。
- 收藏记录不自动删除。

---

# 22. 分享图

## 22.1 内容

- Logo。
- 1–3 张核心牌。
- 一句话主题。
- 日期。
- 问题摘要，可隐藏。
- 免责声明小字。

## 22.2 尺寸

```text
竖版：1080 × 1440
方形：1080 × 1080
```

## 22.3 隐私选择

生成前提供：

- 显示完整问题。
- 只显示问题类别。
- 完全隐藏问题。

## 22.4 分享方式

手机端：

- 优先调用 Web Share API。
- 不支持时下载 PNG。

电脑端：

- 下载 PNG。
- 复制分享链接仅在 P1 开放云端分享后启用。

---

# 23. UI 视觉规范

## 23.1 设计方向

推荐：

> 现代天文台 × 古典牌桌

表现：

- 深蓝黑背景。
- 柔和月光。
- 低饱和金色。
- 古典金属蚀刻边框。
- 大面积留白。
- 低频星尘。
- 高可读正文。

禁止：

- 满屏荧光紫。
- 高频闪烁。
- 过多粒子。
- 骷髅、血液等恐怖元素。
- 低对比度小字。
- 夸张“中奖”动画。
- “100% 准确”等宣传。

## 23.2 色彩 Token

```css
:root {
  --bg-primary: #090b14;
  --bg-secondary: #111526;
  --surface: #171b2e;
  --surface-elevated: #20253b;

  --text-primary: #f5f1e8;
  --text-secondary: #b9b4c7;
  --text-muted: #858197;

  --accent-gold: #d6b46a;
  --accent-moon: #a6afe8;
  --accent-rose: #c98fa6;

  --border: rgba(214, 180, 106, 0.24);
  --overlay: rgba(5, 7, 14, 0.72);

  --danger: #d98282;
  --success: #83b99a;
}
```

## 23.3 字体

标题：

```css
font-family:
  "Noto Serif SC",
  "Songti SC",
  "Source Han Serif SC",
  serif;
```

正文：

```css
font-family:
  Inter,
  "PingFang SC",
  "Microsoft YaHei",
  sans-serif;
```

要求：

- 标题可使用衬线字体。
- 正文必须使用高可读无衬线字体。
- 不允许整页使用装饰字体。
- 牌名和关键词不得使用过细字重。

## 23.4 圆角

```css
--radius-sm: 10px;
--radius-md: 16px;
--radius-lg: 24px;
--radius-card: 18px;
```

---

# 24. 响应式要求

## 24.1 断点

```text
xs：360px
sm：480px
md：768px
lg：1024px
xl：1280px
2xl：1440px+
```

## 24.2 手机端

- 单栏。
- 触控目标至少 44 × 44px。
- 主要操作位于拇指可触区域。
- 弹窗优先使用底部抽屉。
- 选牌使用横向拖动。
- 不依赖 hover。
- 主按钮可使用底部安全区固定栏。
- 兼容 iPhone 安全区域：
  - `env(safe-area-inset-bottom)`。

## 24.3 平板

- 双栏。
- 保留触控尺寸。
- 牌阵和解读可上下或左右排列。

## 24.4 电脑端

- 使用双栏或三栏。
- 牌堆可扇形展开。
- hover 只作为增强。
- 键盘也能完成关键操作。
- 最大内容宽度约 1280px。

---

# 25. 动效规范

## 25.1 基础反馈

时长：

```text
120–220ms
```

用于：

- 按钮。
- 选中。
- 输入校验。
- 小范围展开。

## 25.2 页面转场

时长：

```text
220–400ms
```

用于：

- 页面淡入。
- 抽屉。
- 内容切换。

## 25.3 仪式动效

时长：

```text
600–2200ms
```

用于：

- 洗牌。
- 展牌。
- 翻牌。
- 牌阵落位。

## 25.4 性能要求

- 优先 `transform` 和 `opacity`。
- 同时执行复杂阴影动画的卡牌不超过 12 张。
- 目标 60fps。
- 低性能设备自动降低粒子和阴影。
- 遵守 `prefers-reduced-motion`。
- 页面不可因卡牌图片加载发生明显布局跳动。

---

# 26. API 限流与成本控制

## 26.1 限流

MVP 建议：

- 同一 IP 每分钟最多 5 次生成。
- 同一匿名设备每天最多 20 次生成。
- 超限后显示友好提示。
- 不要在前端仅靠按钮禁用实现限流。
- 服务端必须校验。

## 26.2 请求大小

- 问题最多 200 个中文字符。
- 不把完整 78 张牌资料每次都发送给前端。
- 服务端根据卡牌 ID 加载需要的牌义。
- 限制响应长度。
- 凯尔特十字允许更长输出，其余牌阵限制更短。

## 26.3 缓存

不缓存包含用户私人问题的完整 AI 响应。

可以缓存：

- 牌库静态数据。
- 图片。
- 音效。
- 不包含问题的牌义百科。

---

# 27. 错误与降级

## 27.1 DeepSeek 超时

显示：

> 解读暂时没有完成，你抽到的牌已经保留，可以直接重试。

重试要求：

- 不重新洗牌。
- 不改变正逆位。
- 不改变牌阵。
- 使用相同 `readingId`。

## 27.2 JSON 解析失败

流程：

1. 服务端尝试提取 JSON。
2. Zod 校验。
3. 失败后要求模型修复格式一次。
4. 再失败则使用本地降级模板。
5. 记录匿名错误类型，不记录用户完整问题。

## 27.3 图片失败

- 显示牌名占位图。
- 不阻断解读。
- 提供重试。

## 27.4 音频失败

- 自动切换为静音。
- 不弹出阻断式错误。
- 设置页提示音频暂不可用。

## 27.5 网络断开

- 如果已经抽完牌，保存本地牌局。
- 网络恢复后允许继续请求解读。
- 不要求重新抽牌。

---

# 28. 推荐目录结构

```text
tarot/
├── app/
│   ├── api/
│   │   └── readings/
│   │       └── interpret/
│   │           └── route.ts
│   ├── reading/
│   │   ├── new/
│   │   │   └── page.tsx
│   │   └── [id]/
│   │       └── page.tsx
│   ├── daily/
│   │   └── page.tsx
│   ├── history/
│   │   └── page.tsx
│   ├── settings/
│   │   └── page.tsx
│   ├── about/
│   │   └── page.tsx
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── components/
│   ├── cards/
│   │   ├── TarotCard.tsx
│   │   ├── CardBack.tsx
│   │   ├── CardFan.tsx
│   │   ├── CardDeck.tsx
│   │   └── CardReveal.tsx
│   ├── spreads/
│   │   ├── SingleSpread.tsx
│   │   ├── ThreeCardSpread.tsx
│   │   ├── PentagramSpread.tsx
│   │   └── CelticCrossSpread.tsx
│   ├── audio/
│   │   ├── AudioProvider.tsx
│   │   ├── SoundToggle.tsx
│   │   └── VolumeControl.tsx
│   ├── reading/
│   │   ├── QuestionStep.tsx
│   │   ├── SpreadStep.tsx
│   │   ├── ShuffleStep.tsx
│   │   ├── SelectStep.tsx
│   │   ├── RevealStep.tsx
│   │   └── Interpretation.tsx
│   └── ui/
├── lib/
│   ├── deepseek/
│   │   ├── client.ts
│   │   ├── prompt.ts
│   │   ├── schema.ts
│   │   └── safety.ts
│   ├── tarot/
│   │   ├── shuffle.ts
│   │   ├── spreads.ts
│   │   ├── draw.ts
│   │   └── types.ts
│   ├── audio/
│   │   ├── manager.ts
│   │   └── sounds.ts
│   ├── db/
│   │   └── readings.ts
│   └── rate-limit/
│       └── index.ts
├── src/
│   └── data/
│       └── cards/
│           ├── rider-waite.json
│           ├── thoth.json
│           └── schema.ts
├── public/
│   ├── cards/
│   ├── card-backs/
│   ├── audio/
│   ├── textures/
│   └── icons/
├── tests/
│   ├── shuffle.test.ts
│   ├── cards.test.ts
│   ├── prompt.test.ts
│   └── api.test.ts
├── .env.example
├── .gitignore
├── README.md
├── PRD.md
└── package.json
```

---

# 29. 数据状态机

建议使用以下步骤：

```ts
type ReadingStep =
  | "topic"
  | "question"
  | "spread"
  | "settings"
  | "shuffle"
  | "select"
  | "reveal"
  | "interpreting"
  | "result";
```

牌局状态：

```ts
type ReadingSession = {
  id: string;
  step: ReadingStep;
  question: string;
  topic: string;
  deckSystem: string;
  spreadId: string;
  style: string;
  reversedEnabled: boolean;
  seed: string;
  deckOrder: string[];
  selectedIndexes: number[];
  cards: DrawnCard[];
  interpretation?: ReadingResponse;
  createdAt: string;
};
```

要求：

- 刷新页面后尽可能恢复未完成牌局。
- 进入结果页后，不允许意外重新随机。
- 用户点击“重新抽牌”时必须创建新 `readingId`。

---

# 30. 测试要求

## 30.1 单元测试

必须覆盖：

- 78 张牌无重复。
- 洗牌不会丢牌。
- 抽牌不会重复。
- 同一 Seed 可复现相同牌序。
- 逆位开关生效。
- 关闭逆位后没有逆位牌。
- 每个牌阵位置数量正确。
- Zod 能拒绝错误 AI 输出。
- Sound Manager 在静音时不播放。

## 30.2 组件测试

覆盖：

- 选中牌。
- 撤销选牌。
- 翻牌。
- 全部揭晓。
- 音效开关。
- 高级设置。
- API 超时重试。
- 历史保存。

## 30.3 E2E

至少测试：

1. 手机端完成每日一牌。
2. 手机端完成三牌阵。
3. 电脑端完成凯尔特十字。
4. DeepSeek 超时后保持原牌重试。
5. 关闭声音后全流程无音频。
6. 开启减少动态效果后不播放复杂洗牌动画。
7. 刷新页面后恢复未完成牌局。

## 30.4 设备

最低覆盖：

- iPhone Safari。
- Android Chrome。
- Windows Chrome。
- Windows Edge。
- macOS Safari。
- 360px 宽手机。
- 768px 平板。
- 1440px 桌面。

---

# 31. 性能目标

建议目标：

```text
首屏核心内容快速出现
首屏 JS 尽量精简
卡牌图片按需加载
环境音延迟加载
页面不发生明显布局跳动
洗牌与翻牌目标 60fps
```

优化方式：

- 卡牌图使用现代格式。
- 预加载即将翻开的牌。
- 不在首页加载 78 张完整牌图。
- 音效只预加载核心操作音。
- 环境音用户启用后再加载。
- 牌义数据尽量在服务端按需读取。
- 粒子数量根据设备性能下降。

---

# 32. SEO 与分享元信息

首页：

```text
Title：AI 塔罗抽牌｜沉浸式在线塔罗解读
Description：通过洗牌、选牌和翻牌完成一次私密的塔罗自我探索，支持手机和电脑。
```

要求：

- Open Graph 图片。
- 分享标题不包含用户私人问题。
- 结果页默认不允许搜索引擎索引。
- 动态结果页设置 `noindex`。
- 分享图由用户主动生成。

---

# 33. 分阶段开发计划

## 阶段 1：项目骨架

完成：

- 初始化 Next.js + TypeScript。
- Tailwind。
- 基础布局。
- 深色 Design Token。
- 首页。
- 响应式导航。
- README。
- `.env.example`。

验收：

- 手机和电脑均可打开。
- 首页无横向滚动。
- Lighthouse 无明显基础错误。

## 阶段 2：塔罗数据和随机引擎

完成：

- 牌库 Schema。
- 韦特 78 张数据。
- 洗牌。
- 正逆位。
- 牌阵位置。
- Seed。
- 单元测试。

验收：

- 无重复牌。
- 同一 Seed 可复现。
- 关闭逆位后无逆位。

## 阶段 3：核心抽牌交互

完成：

- 问题输入。
- 牌阵选择。
- 高级设置。
- 洗牌。
- 手机横向选牌。
- 电脑扇形选牌。
- 翻牌。
- 减少动态效果。

验收：

- 手机端可完整抽牌。
- 电脑端可完整抽牌。
- 刷新后牌不变化。

## 阶段 4：音效

完成：

- Audio Provider。
- Sound Manager。
- 音量设置。
- 洗牌、选牌、落位、翻牌、揭晓声音。
- 环境音。
- 页面后台暂停。
- iOS 解锁。

验收：

- 默认不违规自动播放。
- 用户点击后可正常播放。
- 静音后无任何声音。
- 音频失败不影响抽牌。

## 阶段 5：DeepSeek

完成：

- 服务端接口。
- Prompt。
- Zod Schema。
- 安全分类。
- 限流。
- 超时。
- 重试。
- 降级模板。

验收：

- API Key 不出现在浏览器端。
- AI 不改变牌。
- 返回结构稳定。
- 高风险问题被改写或阻止。

## 阶段 6：结果、历史和分享

完成：

- 结果页。
- 单牌解读。
- 牌组互动。
- 行动建议。
- IndexedDB。
- 分享图。
- 隐私选项。

验收：

- 历史可查看和删除。
- 分享图可隐藏问题。
- 结果页手机端可读。

## 阶段 7：部署和验收

完成：

- Vercel 配置。
- 环境变量。
- 错误监控。
- 真实设备测试。
- 版权与许可证检查。
- 隐私与免责声明。

---

# 34. Codex 执行规则

Codex 必须遵守：

1. 先读取本 PRD，再开始写代码。
2. 每个阶段完成后运行测试。
3. 不得把 DeepSeek API Key 写入前端。
4. 不得提交 `.env.local`。
5. 不得用 AI 决定抽牌结果。
6. 不得为了让解读“更准”而重抽。
7. 不得未经确认删除已有代码。
8. 不得使用来源不明的牌图和音效。
9. 不得依赖 hover 完成手机端关键操作。
10. 不得忽略 `prefers-reduced-motion`。
11. 不得只做静态 UI，必须完成真实抽牌状态。
12. 不得在 API 重试时改变用户已抽到的牌。
13. 所有 AI 输出都必须经过 Schema 校验。
14. 所有用户输入都必须在服务端再次校验。
15. 所有重要错误必须给用户可理解的提示。
16. 每个阶段提交一个清晰 Commit。
17. 不要一次性把所有功能堆进一个巨型组件。
18. 抽牌、AI、音效、数据库必须相互解耦。

---

# 35. Codex 首轮开发 Prompt

可直接复制以下内容给 Codex：

```text
请在当前仓库中开发一个手机端优先、同时适配电脑端的塔罗抽牌 Web App。

开始前先完整阅读仓库中的 PRD.md，并严格遵守其中的产品边界、技术架构、安全要求、音效规范和分阶段开发计划。

当前第一阶段只完成：

1. 初始化 Next.js + TypeScript 项目；
2. 配置 Tailwind CSS；
3. 创建全局 Design Token；
4. 创建响应式首页；
5. 创建手机端和电脑端导航；
6. 创建“开始抽牌”和“今日一牌”入口；
7. 创建基础目录结构；
8. 创建 .env.example；
9. 确认 .env.local 已被 gitignore；
10. 创建 README，写明启动方式；
11. 添加基础测试和 lint；
12. 不接入 DeepSeek；
13. 不实现真实抽牌；
14. 不加入来源不明的牌面图片和音效；
15. 完成后运行 lint、typecheck 和测试。

视觉方向：
现代天文台 × 古典牌桌。深蓝黑背景、低饱和金色、柔和月光、少量低频星尘。禁止廉价荧光紫、满屏粒子和恐怖元素。

首页主文案：
“把问题交给牌，把答案留给自己”

副标题：
“一次安静、私密、可回看的塔罗自我探索”

主要按钮：
“开始抽牌”

次要按钮：
“抽取今日一牌”

信任文案：
“无需注册 · 默认不公开问题 · 仅用于娱乐与自我探索”

请保持组件拆分合理，不要把整个页面写进单一组件。完成后汇报：
- 新增文件；
- 主要设计；
- 测试结果；
- 下一阶段建议。
```

---

# 36. 最终验收标准

## 功能

- 用户可以完整完成一次抽牌。
- 抽牌不会重复。
- 正逆位正确。
- DeepSeek 只解读，不改牌。
- API 失败不丢失牌局。
- 历史可保存。
- 分享图可生成。
- 音效可开关。
- 手机和电脑均可使用。

## 体验

- 首次用户不需要说明书。
- 高级设置不会阻挡快速体验。
- 动效顺畅。
- 音效克制。
- 结果可读。
- 不通过恐惧推动用户。
- 不出现确定性预言。

## 安全

- API Key 不泄露。
- 用户问题不出现在 URL。
- 高风险问题不输出具体结论。
- 分享前可隐藏问题。
- 支持清空本地记录。
- 牌图和音效许可清晰。

## 响应式

- 360px 宽无横向溢出。
- 手机端不依赖 hover。
- 桌面端利用大屏空间。
- iOS Safari 音效可在用户手势后正常解锁。
- `prefers-reduced-motion` 生效。

---

# 37. 上线前检查清单

```text
[ ] DeepSeek API Key 只存在于服务端环境变量
[ ] .env.local 未提交
[ ] 牌图许可已确认
[ ] 音效许可已确认
[ ] 每套牌 78 张且无重复
[ ] 抽牌算法测试通过
[ ] 重试不会改变抽牌结果
[ ] 手机端 360px 无横向溢出
[ ] iPhone Safari 测试通过
[ ] Android Chrome 测试通过
[ ] Windows Chrome / Edge 测试通过
[ ] 声音默认符合自动播放规则
[ ] 静音功能正常
[ ] 减少动态效果正常
[ ] 历史记录可删除
[ ] 结果页默认 noindex
[ ] 分享图可以隐藏问题
[ ] 医疗、法律、投资、死亡问题有安全处理
[ ] 隐私说明和免责声明已上线
[ ] API 限流已开启
[ ] 错误日志不记录用户完整问题
```
